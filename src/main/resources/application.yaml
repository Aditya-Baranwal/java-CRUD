server:
 port: 8081

spring:
 datasource:
  url: jdbc:mysql://localhost:3306/lms?useSSL=false
  username: aditya
  password: 1234
 jpa:
  database-platform: org.hibernate.dialect.MySQL8Dialect
  hibernate:
   ddl-auto: update
 mail:
  host: smtp.gmail.com
  port: 587
  username: baranwal.adi@gmail.com
  password: ubow uuaj uqir bpys
  protocol: smtp
  debug: true
  test-connection: false
  default-encoding: UTF-8
  properties.mail.smtp.auth: true
  properties.mail.smtp.starttls.enable: true

springdoc:
  swagger-ui:
   path: /swagger-ui.html
   enabled: true
  api-docs:
   path: /v3/api-docs